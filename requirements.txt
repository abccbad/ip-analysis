# 指定Python版本（关键：用3.10，最稳定）
python_version = "3.10"

# 核心依赖（全部用兼容版本，避免编译）
streamlit==1.32.2
plotly==5.19.0
pandas==2.0.3  # 降级到无需编译的版本
numpy==1.24.3
reportlab==4.1.0
Pillow>=9.5.0,<10.0.0  # 避开10.x的编译问题
